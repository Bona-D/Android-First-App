<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kmech.intern">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Authentication"
        tools:targetApi="31">
        <activity
            android:name=".GridView.GridView"
            android:exported="true" >


        </activity>
        <activity
            android:name=".ListView.Custom_List_View"
            android:exported="true" />
        <activity
            android:name=".ListView.List_View"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".Registration.Registration"
            android:exported="true" />
        <activity
            android:name=".Registration.Home"
            android:exported="true" />
        <activity
            android:name=".Registration.Login"
            android:exported="true" />
        <activity
            android:name=".Registration.SignOut"
            android:exported="true" />
    </application>

</manifest>